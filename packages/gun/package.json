{
  "name": "@chatpuppy/gun",
  "version": "1.0.0",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start_bak": "cross-env NODE_ENV=production DOTENV_CONFIG_PATH=../../.env ts-node -r dotenv/config --transpile-only main.ts",
    "start": "cross-env NODE_ENV=production DOTENV_CONFIG_PATH=../../.env ts-node --transpile-only main.ts",
    "dev:gun": "ts-node --transpile-only main.ts"
  },
  "dependencies": {
    "@chatpuppy/config": "^1.0.0",
    "@chatpuppy/utils": "^1.0.0",
    "gun": "^0.2020.1235",
    "koa": "^2.13.1",
    "event-iterator": "^2.0.0"
  },
  "devDependencies": {
    "svelte": "^3.0.0"
  }
}
